package cpic

import (
//"log"
//"testing"
)

/*
func TestLex(t *testing.T) {
	lex := NewLexer(`tree:
	-> black
		-> red
		-> red`)
	for token := lex.Token(); token.typ != EOF; token = lex.Token() {
		log.Print(token)
	}

}
*/
