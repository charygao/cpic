package cpic

/*
import (
	"log"
	"testing"
)

func TestParse(t *testing.T) {
	p := NewParser(`tree:
	-> black
		-> red
		-> red`)
	n := p.Parse()
	n.walk(func(nd *node) {
		if nd.ele != nil {
			log.Println(nd.ele.(Token), "depth", nd.depth)
		}
	})
}
*/
